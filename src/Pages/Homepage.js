import React from "react";
import Nav from "../Components/Navigation";

const Homepage = () => {
    return (
        <div>
            <Nav />
            <h1>Homepage</h1>
        </div>
    );
};

export default Homepage;